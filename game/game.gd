extends Node2D

#@onready var PlayerOne:Sprite2D = Players.playerOneCharacter;
#@onready var PlayerTwo:Sprite2D = Players.playerTwoCharacter;
# Called when the node enters the scene tree for the first time.
func _ready():
	#PlayerTwo.flip_h;
	#PlayerOne.global_position = Vector2(2,2);
	#PlayerTwo.global_position = Vector2(2,2);
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
